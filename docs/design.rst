Design
======

I'm thinking that I need to put the actual operations of the program in one file.  And then put the click textual interface code in another.  I could also put GUI code in a third.  And even use an jupyter notebook as an interface withh the first file, the actual operations code.
